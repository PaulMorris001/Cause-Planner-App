// Colours used consistently across the widget picker (WidgetBottomSheet)
// and the home screen customize card (WIDGET_META). Keeping them in one place
// means a colour change only needs to happen here.
const widgetColors = {
  classes:  '#2563EB',
  notes:    '#10B981',
  goals:    '#F59E0B',
  calendar: '#EF4444',
} as const;

export default widgetColors;
